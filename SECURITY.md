# セキュリティに関する説明

このドキュメントでは、PDF図表抽出アプリのセキュリティとプライバシーに関する情報を説明します。

---

## 概要

このアプリは、アップロードされたPDFデータを**外部のAI APIに送信しません**。すべての処理はサーバー上でローカルに実行されます。ただし、HuggingFace Spacesなどのクラウド環境にデプロイした場合、PDFデータはそのクラウドサーバーに送信されます。

---

## 外部通信の分析

### 使用ライブラリ

| ライブラリ | 用途 | 外部通信 |
|------------|------|----------|
| Streamlit | Webアプリフレームワーク | なし（テレメトリ無効化済み） |
| Docling | AI図表抽出 | **なし（ローカル実行）** |
| PyMuPDF | PDF検証・処理 | なし |
| PyTorch | AIモデル実行 | なし（推論時） |

### Doclingについて

Docling（IBM製）は**完全にローカルで動作するAIモデル**です。

- PDFデータを外部サーバーに送信しない
- AIモデルは初回起動時にダウンロードされ、以降はローカルで実行
- OpenAI APIやCloudサービスへの依存なし

### Streamlitテレメトリ

Streamlitの使用統計収集は無効化されています。

```toml
# .streamlit/config.toml
[browser]
gatherUsageStats = false
```

---

## データの流れ

### 時系列での説明

| 段階 | 状態 | PDFデータの所在 |
|------|------|----------------|
| 1. アップロード | ブラウザ→サーバー通信 | サーバーに到達 |
| 2. 一時保存 | サーバー上に一時ファイル作成 | サーバーの一時ディレクトリ |
| 3. 処理中 | Doclingが解析 | メモリ上で処理 |
| 4. 抽出完了 | 図・表がメモリに保存される | まだ一時ファイルあり |
| 5. **削除** | 処理ブロック終了 | **一時ファイル削除** |
| 6. 結果表示 | ユーザーに図表表示 | PDFは存在しない |

### 「処理後」の定義

コード上での一時ファイル削除タイミング:

```python
with temporary_pdf_file(bytes(file_content)) as tmp_path:
    # ① PDF処理（Docling変換）
    conv_res = doc_converter.convert(tmp_path)
    # ② 図表抽出
    for element, _level in conv_res.document.iterate_items():
        # 図・表を抽出してリストに保存
    # ③ 結果をセッション状態に保存
    save_processing_result(...)
# ← ④ ここで一時ファイル（PDF）が削除される
```

**「処理後」とは④のタイミング**を指します。具体的には:
- 図表抽出が完了
- 結果がメモリに保存された
- その直後にPDFファイルが自動削除される

---

## HuggingFace Spacesでの注意点

HuggingFace Spacesにデプロイした場合の注意事項:

### データの送信先

PDFをアップロードすると、データは以下の経路を通ります:

```
ユーザーのブラウザ → インターネット → HuggingFaceサーバー → アプリ処理
```

- 通信はHTTPS（暗号化）で保護されています
- ただし、HuggingFaceのサーバー上にデータが一時的に存在します

### 保証できない部分

アプリのコードでは一時ファイルを削除していますが、以下については**保証できません**:

| 項目 | 説明 |
|------|------|
| インフラレベルのログ | HuggingFaceのサーバーログにアクセス記録が残る可能性 |
| サーバーキャッシュ | クラウドインフラのキャッシュ機構 |
| バックアップ | HuggingFaceのバックアップポリシーに依存 |
| 通信経路上のログ | CDNやプロキシのログ |

### HuggingFace Spacesの利用規約

HuggingFace Spacesを利用する場合は、HuggingFaceの利用規約とプライバシーポリシーも適用されます:
- https://huggingface.co/terms-of-service
- https://huggingface.co/privacy

---

## ローカル実行の推奨

### 機密文書を扱う場合

機密性の高い文書（個人情報、企業秘密、医療情報など）を処理する場合は、**ローカル環境での実行を強く推奨**します。

### ローカル実行方法

#### 方法1: Dockerで実行

```bash
# イメージをビルド
docker build -t pdf-extract .

# コンテナを起動
docker run -p 7860:7860 pdf-extract
```

ブラウザで http://localhost:7860 にアクセス

#### 方法2: 直接実行

```bash
# 依存関係をインストール
pip install -r requirements.txt

# アプリを起動
streamlit run app.py --server.port=7860
```

### ローカル実行のメリット

| 項目 | ローカル実行 | クラウド（HuggingFace） |
|------|-------------|----------------------|
| データ送信先 | なし（完全ローカル） | HuggingFaceサーバー |
| 第三者アクセス | 不可能 | HuggingFace運営のみ |
| ログ | 自分で管理 | HuggingFaceが管理 |
| 推奨用途 | 機密文書 | 一般的な文書 |

---

## セキュリティのまとめ

### このアプリが行うこと

- PDFをサーバー上の一時ファイルに保存
- ローカルのAIモデル（Docling）で図表を抽出
- 処理完了後に一時ファイルを自動削除
- 抽出結果をユーザーに提供

### このアプリが行わないこと

- 外部AI API（OpenAI、Google等）へのデータ送信
- PDFデータの永続的な保存
- ユーザー情報の収集
- 使用統計の送信（無効化済み）

---

## 推奨事項

| 文書の種類 | 推奨環境 |
|------------|----------|
| 公開論文、一般資料 | HuggingFace Spaces（問題なし） |
| 社内文書、未公開論文 | ローカル実行を推奨 |
| 個人情報を含む文書 | **ローカル実行必須** |
| 医療・法律関連文書 | **ローカル実行必須** |

---

## 質問・懸念

セキュリティに関する質問や懸念がある場合は、リポジトリのIssuesでお問い合わせください。

---

最終更新: 2026-01-17
